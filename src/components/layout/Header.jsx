import {useState} from "react";import{Menu,X}from"lucide-react";import{NavLink}from"react-router";import AnimatedLogo from"./AnimatedLogo";import{navigationItems}from"../../data/navigation";export default function Header(){const[o,setO]=useState(false);const c=({isActive})=>`premium-nav-link ${isActive?"active":""}`;return <header className="premium-header"><div className="mx-auto flex h-[86px] max-w-[1450px] items-center px-5"><NavLink to="/" aria-label="ProdSecOps home"><AnimatedLogo/></NavLink><nav className="ml-auto hidden gap-1 lg:flex">{navigationItems.map(x=><NavLink key={x.path} to={x.path} className={c}>{x.label}</NavLink>)}<a className="nav-cta" href="mailto:enquiry@vpilot.org">Executive briefing</a></nav><button className="ml-auto lg:hidden" onClick={()=>setO(!o)} aria-expanded={o}>{o?<X/>:<Menu/>}</button></div>{o&&<nav className="mobile-premium-nav lg:hidden">{navigationItems.map(x=><NavLink key={x.path} to={x.path} className={c} onClick={()=>setO(false)}>{x.label}</NavLink>)}</nav>}</header>}
