import { useState } from "react"; import { Menu, X } from "lucide-react"; import { NavLink } from "react-router"; import Brand from "./Brand"; import { navigationItems } from "../../data/navigation";
export default function Header(){const[open,setOpen]=useState(false);const link=({isActive})=>`rounded-lg px-3 py-2 text-xs font-bold ${isActive?'bg-sky-500/10 text-white':'text-slate-400 hover:text-white'}`;return <header className="fixed inset-x-0 top-0 z-50 border-b border-sky-950/80 bg-[#020916]/85 backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-[1450px] items-center px-5"><NavLink to="/"><Brand/></NavLink><nav className="ml-auto hidden gap-1 lg:flex">{navigationItems.map(x=><NavLink key={x.path} to={x.path} className={link}>{x.label}</NavLink>)}</nav><button className="ml-auto rounded-lg border border-sky-900 p-2 lg:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t border-sky-950 bg-[#041425] p-3 lg:hidden">{navigationItems.map(x=><NavLink key={x.path} to={x.path} className={link} onClick={()=>setOpen(false)}>{x.label}</NavLink>)}</nav>}</header>}