import { motion } from "framer-motion"; import { stages } from "../../data/stages"; export default function StageRail({domain,activeStage,setActiveStage}){return <div className="relative grid gap-2 rounded-2xl border border-sky-900 bg-[#061629] p-5 lg:grid-cols-8">{stages.map((name,i)=><button key={name} onClick={()=>setActiveStage(i)} className="text-center"><motion.div animate={{scale:activeStage===i?1.14:1}} className="mx-auto grid h-9 w-9 place-content-center rounded-full border bg-[#071a30] text-[11px] font-extrabold" style={{borderColor:domain.color,color:domain.color,boxShadow:activeStage===i?`0 0 24px ${domain.color}`:'none'}}>0{i+1}</motion.div><b className="mt-2 block text-[11px]">{name}</b><span className="mt-1 block text-[9px] leading-4 text-slate-500">{domain.flow[i]}</span></button>)}</div>}